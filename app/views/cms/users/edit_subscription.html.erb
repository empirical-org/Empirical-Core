<div class='container'>
  <%= react_component('SubscriptionApp', props: {view: 'edit', user: @user, subscription: @subscription, userPremiumTypes: @user_premium_types,
  promoExpiration: @promo_expiration_date, subscriptionPaymentMethods: @subscription_payment_methods, purchaserEmail: @purchaser_email, schoolsUsers: @schools_users })%>

  <!-- <article class='simple-rounded-box cms-container'>
    <%= link_to 'User Directory', cms_users_path %> > <%= link_to @user.name, cms_user_path(@user.id) %> > Edit Subscription
    <br /><br />
    <h2>Edit Subscription: <%= @user.name %></h2>
    <br />
    <div class='cms-form'>
      <%= form_tag update_subscription_cms_user_path(params[:id]) do %>
        <div class='cms-form-row'>
          <%= label_tag 'premium_status', 'Premium Status' %>
          <%= select_tag 'premium_status', options_for_select(@user_premium_types, @account_type), include_blank: true, autocomplete: 'off' %>
        </div>

        <div class='cms-form-row'>
          <%= label_tag 'payment_method', 'Payment Method' %>
          <%= select_tag 'payment_method', options_for_select(@subscription_payment_methods, @payment_method), include_blank: true, autocomplete: 'off' %>
        </div>

        <div class='cms-form-row'>
          <%= label_tag 'payment_amount', 'Payment Amount In Cents' %>
          <span>e.g. $450 = 45000</span>
          <%= number_field_tag('payment_amount') %>
        </div>

        <div class='cms-form-row'>
          <%= label_tag 'start_date', 'Start Date' %>
          <%= select_date @start_date, prefix: :start_date, autocomplete: 'off' %>
        </div>

        <div class='cms-form-row'>
          <%= label_tag 'expiration_date', 'Expiration Date' %>
          <%= select_date @expiration_date, prefix: :expiration_date, autocomplete: 'off' %>
        </div>

        <div class='cms-form-row'>
          <%= label_tag 'purchaser_email', 'Purchaser Email' %>
          <%= text_field_tag @purchaser_email %>
        </div>

        <div class='cms-submit-row'>
          <%= submit_tag 'Save' %><br /><br />
          <%= link_to 'Cancel', cms_user_path(@user.id)  %>
        </div>
      <% end %>
    </div>
  </article> -->
</div>
