!!!
%html
  %head
    %title PGSite
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'https://www.google.com/jsapi'
    = javascript_include_tag 'http://demonstrations.wolfram.com/javascript/embed.js'
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body
    = render 'layouts/header'
    .container
      .main-wrap
        %aside.video= yield(:video)
        .whiteboard= yield(:whiteboard)
        %ul.controls.primary-controls
          %li.active= link_to 'graph', '/', class: 'graph'
          - %w(textbook conversation search folder citation notes).each do |icon|
            %li= link_to icon, '/', class: icon
        .separator
        %article.text-area= yield(:text_area)
        %ul.controls.text-area-controls
          - %w(annotation nextpage).each do |icon|
            %li= link_to icon, '/', class: icon
