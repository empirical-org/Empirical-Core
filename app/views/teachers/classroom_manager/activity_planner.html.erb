

<%= render partial: 'teachers/shared/scorebook_tabs' %>



<div id="page-content-wrapper">

	<div class="tab-pane" id="activity-planner" data-tab="<%= @tab %>" data-grade="<%= @grade %>" data-students="<%= @students_exist %>" data-classroom-name="<%= @last_classroom_name%>" data-classroom-id="<%= @last_classroom_id%>">
    <%= render partial: 'application/spinner' %>

	<!-- ==========================================================================
		 Progress tracker
		========================================================================== -->

	</div>
</div> <!-- //#page-content-wrapper -->
