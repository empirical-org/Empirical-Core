import * as React from 'react';
import { connect } from 'react-redux';

export default class CustomizeLesson extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return <div>Customize Lesson Page</div>
  }
}
