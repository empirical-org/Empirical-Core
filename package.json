{
  "name": "quill-connect-db",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf lib && mkdir lib && $(npm bin)/babel src --out-dir lib --presets es2015,stage-2",
    "start": "node lib/index.js",
    "start:dev": "nodemon index.js --exec babel-node --presets es2015,stage-2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.11.0",
    "rethinkdb": "^2.3.3",
    "socket.io": "^2.0.3"
  }
}
