<div class="container">

  <p id="notice"><%= notice %></p>

  <h1>Question Sets</h1>

  <div class="card">
    <div class="table-responsive">
      <table class="table table-hover table-outline table-vcenter card-table">
        <thead>
          <tr>
            <th>Activity</th>
            <th>Prompt</th>
            <th>Order</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @question_sets.each do |question_set| %>
            <tr>
              <td><%= link_to question_set.activity&.title, activity_path(question_set.activity_id) %></td>
              <td><%= question_set.prompt %></td>
              <td><%= question_set.order %></td>
              <td><%= link_to 'Show', question_set %></td>
              <td><%= link_to 'Edit', edit_question_set_path(question_set) %></td>
              <td><%= link_to 'Destroy', question_set, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <%= link_to 'New Question Set', new_question_set_path %>

</div>