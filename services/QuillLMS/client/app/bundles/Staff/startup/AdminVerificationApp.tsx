import React from 'react';

import AdminVerification from '../containers/AdminVerification';

const AdminVerificationApp = (props) => (
  <AdminVerification {...props} />
);

export default AdminVerificationApp
