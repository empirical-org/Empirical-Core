import * as React from 'react';

import SpringBoard from '../../Teacher/containers/SpringBoardContainer';

const SpringBoardApp = (props) => (
  <SpringBoard {...props} />
);

export default SpringBoardApp;
