import React from 'react';
import { shallow } from 'enzyme';

import LessonPlanner from '../LessonPlanner.jsx';

describe.skip('LessonPlanner container', () => {

  //TODO: write these tests
  //TODO: also, write tests for lifecycle events
  //TODO: also, write tests for each of the functions we're passing down

  describe('on create unit tab', () => {
    it('should render CreateUnit component with relevant data, actions, and analytics props', () => {

    });
  });

  describe('on assign new activity tab', () => {
    it('should render AssignANewActivity component with toggleTab function and flag', () => {

    });
  });

  describe('on manage units tab', () => {
    it('should render ManageUnits component with toggleTab and editUnit actions', () => {

    });
  });

});
