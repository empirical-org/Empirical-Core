---
:concurrency: <%= ENV.fetch("RAILS_MAX_THREADS") { 4 } %>
staging:
  :concurrency: <%= ENV.fetch("RAILS_MAX_THREADS") { 4 } %>
production:
  :concurrency: <%= ENV.fetch("RAILS_MAX_THREADS") { 4 } %>
:queues:
  - critical
  - default
  - low
