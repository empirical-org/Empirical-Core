Description:
    Generate an empty model. Uses the syntax of rails scaffold generators.

Example:
    rails generate quill_model FakeModel name:string level:integer

    This will create:
        model
