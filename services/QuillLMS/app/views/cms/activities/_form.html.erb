<%= react_component('ActivityFormIndex', props: {
  activity: @activity,
  activityClassification: @activity_classification,
  contentPartnerOptions: ContentPartner.all,
  activityCategoryOptions: ActivityCategory.all,
  standardOptions: Standard.all,
  rawScoreOptions: RawScore.all,
  passedTopicOptions: Topic.all,
  flagOptions: Activity::FLAGS,
  followUpActivityOptions: Activity.all
}) %>
