<% current_path = request.env['PATH_INFO'] %>

<% if viewing_demo_account? %>
  <div class="preview-student-banner">
    <div class="content-container">
      <span>Viewing a demo account. Changes you make will not be saved.</span>
      <% if signed_in_outside_demo? %>
        <a class="quill-button primary contained medium focus-on-light" href="/teachers/unset_view_demo">Back to My Dashboard</a>
      <% else %>
        <a class="quill-button primary contained medium focus-on-light" href="/session">Exit Demo Account</a>
      <% end %>
    </div>
  </div>

<% end %>
