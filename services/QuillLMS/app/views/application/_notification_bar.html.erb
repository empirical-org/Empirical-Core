<% current_path = request.env['PATH_INFO'] %>
<% if current_path.include?('session')  %>
<div class="notification-bar google-issue">
  <p>
    Unfortunately, we’re experiencing an issue with the ‘Log in with Google’ feature, which may cause a small number of users to see a 403 error when logging in. Both Quill and Google engineers are working to resolve the issue as quickly as possible. We&nbsp;apologize&nbsp;for&nbsp;the&nbsp;inconvenience!
  </p>
</div>
<% end %>
