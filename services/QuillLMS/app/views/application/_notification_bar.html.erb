<% current_path = request.env['PATH_INFO'] %>
<% if ['account/new', '/session', 'sign-up'].any? { |str| current_path.include?(str)} %>
<div class="notification-bar google-issue">
  <p>
    Unfortunately, we’re experiencing an issue with our Google integration, which may cause a small number of users to see a 403 error when logging in or signing up with Google. Both Quill and Google engineers are working to resolve the issue as quickly as possible. We&nbsp;apologize&nbsp;for&nbsp;the&nbsp;inconvenience!
  </p>
</div>
<% end %>
