<%= react_component('AccountSettingsApp', props: {
    role: current_user.role,
    name: current_user.name,
    email: current_user.email,
    userName: current_user.username,
    googleId: current_user.google_id,
    cleverId: current_user.clever_id,
    timeZone: current_user.time_zone,
    signedUpWithGoogle: current_user.signed_up_with_google,
    accountType: current_user.account_type,
    isBeingPreviewed: previewing_student_dashboard?,
   }) %>
