<!DOCTYPE html>
<html>
  <head>
    <title>Quill</title>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
    <link rel="stylesheet" href="https://use.typekit.net/zyx1nqp.css">
    <style type="text/css">
      main {
        font-family: adelle-sans, Arial, sans-serif;
        padding: 0px 64px;
        zoom: 60%;
      }

      .quill-logo {
        height: 50px;
        margin-bottom: 56px;
      }

      .header {
        display: flex;
        justify-content: space-between;
        width: 100%;
      }

      h1 {
        font-size: 24px;
        margin-bottom: 32px;
      }

      /* Applied Filters Styles */
      .applied-filters {
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #E8AB88;
        background-color: #FFF6F1;
      }

      .applied-filters h3 {
        color: #FFF;
        font-size: 9px;
        font-weight: 700;
        text-transform: uppercase;
        background-color: #C04500;
        width: min-content;
        white-space: nowrap;
        padding: 4px 5px 3px;
        border-radius: 5px;
        margin: 0px;
      }

      .applied-filters p {
        color: #5C5C5C;
        font-size: 14px;
        margin: 8px 0px 24px;
      }

      /* Filter Section Styles */
      .filter-sections {
        display: flex;
        gap: 16px;
      }

      .pill {
        display: block;
        border-radius: 16px;
        background: #06806b;
        padding: 6px 10px;
        color: white;
        margin-bottom: 8px;
        width: fit-content;
        font-size: 12px;
        line-height: 16px;
      }

      .filter-section {
        flex: 1;
        border-radius: 8px;
        border: 1px solid #E8AB88;
        background: #F6E5DC;
        padding: 8px 16px;
      }

      .filter-section-title {
        margin-top: 0px;
        color: #000;
        font-size: 14px;
        line-height: 22px;
        margin-bottom: 8px;
      }

      /* Snapshot Section Styles */
      .snapshot-section-wrapper {
        margin-top: 32px;
        padding: 0px;
        page-break-inside: avoid;
      }

      .snapshot-section-wrapper h2 {
        font-weight: 600;
        font-size: 20px;
        line-height: 32px;
        color: #373737;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
      }

      .snapshot-section-wrapper h2 img {
        height: 24px;
        margin-right: 8px;
      }

      .snapshot-section-wrapper:first-of-type {
        margin-top: 36px;
      }

      .snapshot-section {
        border-radius: 8px;
        padding: 24px;
        border: 1px solid #e0e0e0;
      }

      .snapshot-section .snapshot-item {
        background-color: #f8f8f8;
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        display: flex;
        justify-content: space-between;
        min-height: min-content;
        position: relative;
        page-break-inside: avoid;
      }

      .snapshot-section .snapshot-item h3 {
        font-weight: 700;
        font-size: 18px;
        line-height: 22px;
        margin: 0px;
      }

      /* Count and Label Styles */
      .snapshot-section .snapshot-item .count-and-label {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .snapshot-section .snapshot-item .count {
        font-weight: 700;
        font-size: 28px;
        line-height: 34px;
      }

      .snapshot-section .snapshot-item .snapshot-label {
        font-weight: 600;
        color: #373737;
      }

      /* Snapshot Item Change Styles */
      .snapshot-section .snapshot-item .change {
        font-weight: 700;
        font-size: 20px;
        line-height: 24px;
        color: #373737;
        display: flex;
        align-items: center;
        height: min-content;
      }

      .snapshot-section .snapshot-item .change img {
        margin-right: 4px;
      }

      .snapshot-section .snapshot-item .change img.small {
        height: 26px;
      }

      .snapshot-section .snapshot-item .change img.medium {
        height: 31px;
      }

      .snapshot-section .snapshot-item.snapshot-count .loading-spinner-wrapper {
        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
      }

      /* Snapshot Item Size Variants */
      .snapshot-section .snapshot-item.small {
        padding: 16px;
        height: 133px;
      }

      .snapshot-section .snapshot-item.small .snapshot-label {
        font-size: 14px;
        line-height: 18px;
      }

      .snapshot-section .snapshot-item.medium {
        padding: 32px;
        height: 137px;
      }

      .snapshot-section .snapshot-item.medium .snapshot-label {
        font-size: 16px;
        line-height: 21px;
      }

      /* Snapshot Item State Styles */
      .snapshot-section .snapshot-item.positive,
      .snapshot-section .snapshot-item.negative,
      .snapshot-section .snapshot-item.no-change {
        color: #373737;
      }

      .snapshot-section .snapshot-item.positive {
        background-color: #f1faf6;
        border: 1px solid #b4e6d1;
      }

      .snapshot-section .snapshot-item.positive .change {
        color: #129e6c;
      }

      .snapshot-section .snapshot-item.negative .change {
        color: #c38d33;
      }

      /* Snapshot Feedback Styles */
      .snapshot-section .snapshot-feedback {
        display: flex;
        padding: 16px;
        flex-direction: column;
        grid-column: span 3;
        justify-content: flex-start;
        color: inherit;
        text-decoration: none;
      }

      .snapshot-section .snapshot-feedback .header {
        margin-bottom: 8px;
      }

      .snapshot-section .snapshot-feedback p {
        font-weight: 400;
        font-size: 14px;
        line-height: 20px;
        color: #373737;
      }

      .snapshot-section .snapshot-feedback img {
        height: 20px;
      }

      .snapshot-section .snapshot-feedback:hover,
      .snapshot-section .snapshot-feedback:focus {
        text-decoration: none;
      }

      /* Snapshot Ranking Styles */
      .snapshot-section .snapshot-ranking {
        padding: 32px;
        display: flex;
        flex-direction: column;
      }

      .snapshot-section .snapshot-ranking .header {
        margin-bottom: 26px;
      }

      .snapshot-section .snapshot-ranking .table {
        width: 100%;
        margin-bottom: 0px;
      }

      /* Ranking Row Styles */
      .snapshot-section .snapshot-ranking .header-row {
        border-top: 1px solid #aeaeae;
      }

      .snapshot-section .snapshot-ranking .header-row th {
        font-weight: 700;
        font-size: 14px;
        line-height: 17px;
        color: #373737;
      }

      .snapshot-section .snapshot-ranking .header-row,
      .snapshot-section .snapshot-ranking .data-row {
        padding: 12px 0px;
        display: flex;
        justify-content: space-between;
        color: #373737;
        font-size: 14px;
      }

      .snapshot-section .snapshot-ranking .data-row {
        border-top: 1px solid #e0e0e0;
      }

      .snapshot-section .snapshot-ranking .data-row td:first-of-type {
        font-weight: 400;
        line-height: 16px;
        margin-right: 20px;
      }

      .snapshot-section .snapshot-ranking .data-row td:last-of-type {
        font-weight: 700;
        line-height: 17px;
      }

      .snapshot-section .snapshot-ranking.has-data {
        cursor: pointer;
      }

      /* Responsive Grid Styles for Various Sections */
      .snapshot-section.highlights .counts,
      .snapshot-section.users .snapshot-section-content,
      .snapshot-section.classrooms .snapshot-section-content,
      .snapshot-section.users .counts,
      .snapshot-section.classrooms .counts {
        display: grid;
        gap: 16px;
      }

      /* Specific Grid Template Styles */
      .snapshot-section.highlights .counts {
        grid-template-columns: repeat(2, 1fr);
      }

      .snapshot-section.users .snapshot-section-content,
      .snapshot-section.classrooms .snapshot-section-content {
        grid-template-columns: repeat(2, 1fr);
      }

      .snapshot-section.users .counts,
      .snapshot-section.classrooms .counts {
        grid-template: repeat(2, 1fr) / repeat(2, 1fr);
      }

      .snapshot-section.practice .snapshot-section-content {
        display: flex;
        flex-direction: column;
      }

      .snapshot-section.practice .first-row,
      .snapshot-section.practice .second-row,
      .snapshot-section.practice .third-and-fourth-row {
        margin-bottom: 16px;
        gap: 16px;
      }

      .snapshot-section.practice .first-row {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
      }

      .snapshot-section.practice .second-row,
      .snapshot-section.practice .fifth-row {
        display: grid;
        grid-template-columns: none;
        gap: 16px;
      }

      .snapshot-section.practice .third-and-fourth-row {
        display: grid;
        grid-template: repeat(2, 1fr) / repeat(4, 1fr);
      }

      /* Adjusting Grid Layouts */
      .snapshot-section .snapshot-feedback {
        grid-column: inherit;
      }

      .snapshot-section.users .snapshot-section-content,
      .snapshot-section.classrooms .snapshot-section-content {
        display: flex;
        flex-direction: column;
      }

      .snapshot-section.practice .third-and-fourth-row {
        grid-template: repeat(3, 1fr)/repeat(2, 1fr);
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
