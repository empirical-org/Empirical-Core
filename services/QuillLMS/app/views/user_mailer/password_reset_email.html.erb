<p>Click on the link below to reset your password (expires in 24 hours):</p>

<a href=<%= "#{ENV['DEFAULT_URL']}/password_reset/#{@user.token}" %>><%= "#{ENV['DEFAULT_URL']}/password_reset/#{@user.token}" %></a>
