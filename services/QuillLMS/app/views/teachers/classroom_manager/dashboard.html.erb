<%= render partial: 'teachers/shared/scorebook_tabs' %>

<%= react_component('DashboardApp', props: {
  linkedToClever: current_user.clever_id.present?,
  mustSeeWelcomeModal: @must_see_welcome_modal,
  mustSeeTeacherInfoModal: @must_see_teacher_info_modal,
  featuredBlogPosts: @featured_blog_posts,
  onboardingChecklist: @objective_checklist,
  firstName: @first_name,
  showEvidencePromotionCard: current_user.teaches_eighth_through_twelfth?,
  subjectAreas: SubjectArea.all
}) %>

<% if current_user.show_satismeter? %>
  <%= render partial: 'application/satismeter.html.erb' %>
<% end %>

<%= react_component('ArticleSpotlightApp', props: { featuredBlogPost: @featured_blog_post, featuredBlogPostAuthor: @featured_blog_post_author }) %>
