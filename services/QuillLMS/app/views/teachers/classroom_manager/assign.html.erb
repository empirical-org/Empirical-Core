<%= render partial: 'teachers/shared/scorebook_tabs' %>
<%= render partial: 'pages/shared/navbar_divider' %>
<%= react_component('AssignActivitiesApp', props: {
  grade: @grade.to_s,
  tab: @tab.to_s,
  classroomName: @last_classroom_name,
  classroomId: @last_classroom_id.to_s,
  cleverLink: @clever_link,
  students: @students.to_s,
  numberOfActivitiesAssigned: @number_of_activities_assigned,
  showDiagnosticBanner: @show_diagnostic_banner,
  showEvidenceBanner: @show_evidence_banner,
  showLessonsBanner: @show_lessons_banner,
  showGradeLevelWarning: @show_grade_level_warning,
  assignedPreTests: @assigned_pre_tests,
  flagset: current_user&.flagset,
  referralCode: current_user&.referral_code,
  user: current_user
})%>
